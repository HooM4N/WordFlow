services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wordflow_dashboard
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1